<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android_projet">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Android_projet">
        <activity
            android:name=".MemoriActivity"
            android:exported="false" />
        <activity
            android:name=".controller.StatisticsActivity"
            android:exported="false" />
        <activity
            android:name=".controller.ProfileSelectionActivity"
            android:exported="false" />
        <activity
            android:name=".controller.MenuActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>